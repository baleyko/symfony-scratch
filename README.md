# Symfony Scratch Edition - nothing new, it's just a skeleton for rapid start

```shell
$ git clone https://github.com/baleyko/symfony-scratch.git
$ docker-compose up
$ curl -v http://YOUR_DOCKER_MACHINE_IP/
```
